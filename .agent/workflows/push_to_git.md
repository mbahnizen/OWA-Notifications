---
description: Push changes to git
---
// turbo-all
1. Add all changes
```
git add .
```
2. Commit changes
```
git commit -m "Redesign options page, add tag input for folders, and update defaults"
```
3. Push to remote
```
git push origin master
```
